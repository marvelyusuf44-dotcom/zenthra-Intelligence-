{
  "framework": null,
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/api-server run build && BASE_PATH=/app/ pnpm --filter @workspace/zenthra run build && mkdir -p combined/app && cp -r artifacts/zenthra/dist/public/. combined/app/ && cp artifacts/landing/index.html combined/index.html",
  "outputDirectory": "combined",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/app", "destination": "/app/index.html" },
    { "source": "/app/(.*)", "destination": "/app/index.html" }
  ]
}
